{
  "name": "adorn",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm --prefix server start\" \"cmd /c set NODE_OPTIONS=--no-deprecation && npm --prefix client start\"",
    "build": "npm --prefix client run build",
    "seed": "node server/seed.js",
    "start": "node --no-deprecation server/index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
